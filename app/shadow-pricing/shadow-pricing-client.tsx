"use client";
import {useMemo,useState} from "react";
import Decimal from "decimal.js";
import {Calculator,Save,ShieldAlert,SlidersHorizontal} from "lucide-react";
import {Badge} from "@/components/ui/badge";
import {Button} from "@/components/ui/button";
import {Card,CardContent,CardHeader} from "@/components/ui/card";
import {KpiCard,PageHeading} from "@/components/page-parts";
import {authorizedFetch} from "@/lib/authorized-fetch";
import {calculateShadowQuote} from "@/lib/domain";
import {formatRate,formatVnd} from "@/lib/utils";

type Initial={sources:Array<{id:string;source_type:string;settleable_available_amount_vnd:number|string;funding_rate_vnd_per_usdt:number|string|null;allocationRatio:number}>;merchants:string[];channels:string[];replacementRate:{rate_vnd_per_usdt:number|string;rate_date:string;time_precision:string;confidence:string}|null;dataCompletenessStatus:string};
export function ShadowPricingClient({initial}:{initial:Initial}){const [received,setReceived]=useState("1000");const [currentAs,setCurrentAs]=useState("25825.50");const [fee,setFee]=useState("100000");const [replacement,setReplacement]=useState(String(initial.replacementRate?.rate_vnd_per_usdt??"26628"));const [merchant,setMerchant]=useState(initial.merchants[0]??"");const [channel,setChannel]=useState(initial.channels[0]??"");const [message,setMessage]=useState("");const economicCostPerVnd=useMemo(()=>initial.sources.reduce((sum,source)=>{const rate=source.source_type==="TOPUP"&&source.funding_rate_vnd_per_usdt?new Decimal(source.funding_rate_vnd_per_usdt):new Decimal(replacement||1);return sum.plus(new Decimal(source.allocationRatio).div(rate));},new Decimal(0)).toFixed(18),[initial.sources,replacement]);const min=useMemo(()=>calculateShadowQuote({receivedUsdt:received||"1",economicCostPerVnd,fixedPayoutFeeVnd:fee||"0",targetMargin:"0.002",currentAsRate:currentAs||undefined}),[received,economicCostPerVnd,fee,currentAs]);const target=useMemo(()=>calculateShadowQuote({receivedUsdt:received||"1",economicCostPerVnd,fixedPayoutFeeVnd:fee||"0",targetMargin:"0.005",currentAsRate:currentAs||undefined}),[received,economicCostPerVnd,fee,currentAs]);async function save(){setMessage("保存中…");try{const response=await authorizedFetch("/api/shadow-pricing",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({receivedUsdt:received,merchant,channel,currentAsRate:currentAs,estimatedPayoutFeeVnd:fee,economicCostPerVnd,replacementRate:replacement})});const body=await response.json();if(!response.ok)throw new Error(body.message);setMessage(`Shadow Run v${body.run.run_version} 已保存，不触发任何资金操作。`);}catch(error){setMessage(error instanceof Error?error.message:"保存失败");}}
return <><PageHeading title="VND 影子报价" subtitle="在满足利润目标的前提下，给出尽可能高的 VND/USDT 报价" actions={<Button onClick={save}><Save size={14}/>保存为 Shadow Run</Button>}/><div className="alert alert-warning" style={{marginBottom:16}}><ShieldAlert size={16}/><div><strong>{initial.dataCompletenessStatus}</strong>Account History 截止早于 Payout 与补U数据；结果为影子估算，不修改真实商户报价，不执行付款或补U。</div></div><div className="grid-2"><Card><CardHeader><div><h2 className="panel-title">报价输入</h2><div className="panel-subtitle">汇率方向统一为 VND_PER_USDT</div></div><SlidersHorizontal size={16}/></CardHeader><CardContent><div className="pricing-form-grid"><label>收到 USDT<input value={received} onChange={e=>setReceived(e.target.value)} inputMode="decimal"/></label><label>当前人工 AS<input value={currentAs} onChange={e=>setCurrentAs(e.target.value)} inputMode="decimal"/></label><label>预计 Payout 手续费 VND<input value={fee} onChange={e=>setFee(e.target.value)} inputMode="decimal"/></label><label>替代汇率 VND/USDT<input value={replacement} onChange={e=>setReplacement(e.target.value)} inputMode="decimal"/></label><label>商户<select value={merchant} onChange={e=>setMerchant(e.target.value)}>{initial.merchants.map(value=><option key={value}>{value}</option>)}</select></label><label>通道<select value={channel} onChange={e=>setChannel(e.target.value)}>{initial.channels.map(value=><option key={value}>{value}</option>)}</select></label></div>{message&&<div className="alert alert-info" style={{marginTop:14}}>{message}</div>}</CardContent></Card><Card><CardHeader><div><h2 className="panel-title">付款前资金来源</h2><div className="panel-subtitle">按 available_settleable_balance_vnd 占比</div></div><Badge variant="violet">{initial.sources.length} 个桶</Badge></CardHeader><CardContent><div className="metric-list">{initial.sources.map(source=><div className="metric-row" key={source.id}><span className="metric-label">{source.source_type}</span><span className="metric-value">{(source.allocationRatio*100).toFixed(2)}% · {formatVnd(source.settleable_available_amount_vnd)}</span></div>)}</div></CardContent></Card></div><div className="kpi-grid"><KpiCard label="当前人工 AS" value={formatRate(currentAs)} note={<span>越高则商户获得 VND 越多</span>} icon={Calculator}/><KpiCard label="千2保护报价" value={formatRate(min.recommendedAsRateVndPerUsdt)} note={<span>最低经济利润率 0.2%</span>} icon={Calculator} color="#dc8b16"/><KpiCard label="千5目标报价" value={formatRate(target.recommendedAsRateVndPerUsdt)} note={<span>目标经济利润率 0.5%</span>} icon={Calculator} color="#0f9f78"/><KpiCard label="当前预计经济利润率" value={`${(Number(min.currentEconomicMargin??0)*100).toFixed(3)}%`} note={<span>ESTIMATED · 非已实现净利润</span>} icon={Calculator} color="#6f4bb7"/></div><Card><CardHeader><div><h2 className="panel-title">报价差异</h2><div className="panel-subtitle">手续费规则需人工输入；TOPUP 汇率精度为 DATE_ONLY</div></div><Badge variant="amber">LOW CONFIDENCE</Badge></CardHeader><CardContent><div className="metric-list"><div className="metric-row"><span className="metric-label">千2最大商户本金</span><span className="metric-value">{formatVnd(min.maxMerchantPrincipalVnd)}</span></div><div className="metric-row"><span className="metric-label">千5最大商户本金</span><span className="metric-value">{formatVnd(target.maxMerchantPrincipalVnd)}</span></div><div className="metric-row"><span className="metric-label">当前报价是否低于千2</span><Badge variant={Number(min.currentEconomicMargin)>=.002?"green":"red"}>{Number(min.currentEconomicMargin)>=.002?"否":"是"}</Badge></div><div className="metric-row"><span className="metric-label">当前报价是否达到千5</span><Badge variant={Number(min.currentEconomicMargin)>=.005?"green":"amber"}>{Number(min.currentEconomicMargin)>=.005?"是":"否"}</Badge></div></div></CardContent></Card></>}
